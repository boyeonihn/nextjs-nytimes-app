import { Grandiflora_One, Berkshire_Swash, Ultra } from 'next/font/google';

export const grandi = Grandiflora_One({
  weight: '400',
  subsets: ['latin', 'latin-ext'],
});

export const ultra = Ultra({
  weight: '400',
  subsets: ['latin'],
});

export const berkshire = Berkshire_Swash({
  weight: '400',
  subsets: ['latin', 'latin-ext'],
});
